package repository.interfaces;

import model.users.Patient;

/**
 * The interface that defines the contract for a patient repository.
 */
public interface IPatientRepository extends IRepository<Patient> {

    
}